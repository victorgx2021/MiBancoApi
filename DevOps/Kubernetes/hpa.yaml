apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: mibanco-api-hpa
  namespace: mibanco
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: mibanco-api
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
